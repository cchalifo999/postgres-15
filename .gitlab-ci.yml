image: docker:stable


stages:
  - build
  

#services:
# - docker:dind
 #  command: ["--tls=false"]



before_script:
  - docker info
  - echo $op | docker login -u $CI_REGISTRY_USER --password-stdin $CI_REGISTRY/$CI_REGISTRY_USER 



Build-postgress_ssl:
  stage: build
  
  script:
 
  - docker build -t $CI_REGISTRY/$CI_REGISTRY_USER/postgres15:latest .
  - docker push $CI_REGISTRY/postgres15:latest 

